#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n; cin>>n;
    if(n*200 < 1000)
    {
        cout<<n*200<<"\n";
    }
    else
    {
        cout<<1000<<"\n";
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

  
        solve();
    

    return 0;
}