#include <bits/stdc++.h>
using namespace std;

void solve() {
    int a,b; cin>>a>>b;
    float f= (float)a/2;
    if(b>=f) cout<<"NEWBIE\n";
    else cout<<"PRO\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    
        solve();
    

    return 0;
}