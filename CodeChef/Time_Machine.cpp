#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n; cin >> n;
    if(n >= 2050 || (n+25) >= 2050)
    {
        cout<<"Yes\n";
    }
    else
    {
        cout<<"No\n";
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    
        solve();
    

    return 0;
}