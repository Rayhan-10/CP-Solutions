#include <bits/stdc++.h>
using namespace std;

void solve() {
    int a,b;
    cin>>a>>b;
    cout<<a+(b*10)<<"\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // int TC;
    // cin >> TC;
    // while (TC--) {
        solve();
    // }

    return 0;
}